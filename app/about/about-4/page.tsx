import { AboutFourView } from "@/views"
import { Metadata } from "next"

export const metadata: Metadata = {
    title: "About Us - Style 4",
}

export default function page() {
    return <AboutFourView />
}
