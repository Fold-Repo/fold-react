import { API_BASE_URL, ENDPOINT } from "./endpoint";
import { CONTACT_INFO, type CONTACT_INFO_TYPE, type CONTACT_MAP_INFO_TYPE , CONTACT_MAP_INFO} from "./contact";
import { RETURN_POLICY_CONTENT } from "./returnPolicy";
import { SHIPPING_RETURN_CONTENT } from "./shippingReturn";

export {
    API_BASE_URL,
    ENDPOINT,

    CONTACT_INFO,
    CONTACT_INFO_TYPE,
    CONTACT_MAP_INFO_TYPE,
    CONTACT_MAP_INFO,

    RETURN_POLICY_CONTENT,
    SHIPPING_RETURN_CONTENT
}